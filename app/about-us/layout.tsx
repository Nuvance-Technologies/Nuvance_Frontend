import { ReactNode } from "react";

export const metadata = {
    title: "About Us | Nuvance Technologies",
    description: "Learn and got to know more about Nuvance Technologies, our mission, vision, and our expert IT team.",
};

export default function AboutLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
