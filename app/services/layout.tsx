import { ReactNode } from "react";

export const metadata = {
    title: "Our Services | Nuvance Technologies",
    description: "Explore a wide range of IT services including web development, mobile apps, cloud, and AI solutions.",
};

export default function ServicesLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}