import { ReactNode } from "react";

export const metadata = {
    title: "Contact Us | Nuvance Technologies",
    description: "Get in touch with Nuvance Technologies for expert IT solutions and consultation.",
};

export default function ContactLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}